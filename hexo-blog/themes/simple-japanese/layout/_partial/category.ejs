<% if (post.categories && post.categories.length){ %>
<i class="fas fa-folder" style="margin-left: 5px;"></i>
<span class="category">
  <% post.categories.forEach(category => { %>
  <a class="category" href="<%- url_for(category.path) %>"><%= category.name %></a>
  <% }) %>
</span>
<% } %>
